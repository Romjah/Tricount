{% extends 'base.html.twig' %}

{% block title %}ExpenseAttachment{% endblock %}

{% block body %}
    <h1>ExpenseAttachment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ expense_attachment.id }}</td>
            </tr>
            <tr>
                <th>File</th>
                <td>{{ expense_attachment.file }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_expense_attachment_index') }}">back to list</a>

    <a href="{{ path('app_expense_attachment_edit', {'id': expense_attachment.id}) }}">edit</a>

    {{ include('expense_attachment/_delete_form.html.twig') }}
{% endblock %}
