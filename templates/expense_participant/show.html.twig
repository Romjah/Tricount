{% extends 'base.html.twig' %}

{% block title %}ExpenseParticipant{% endblock %}

{% block body %}
    <h1>ExpenseParticipant</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ expense_participant.id }}</td>
            </tr>
            <tr>
                <th>Share</th>
                <td>{{ expense_participant.share }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_expense_participant_index') }}">back to list</a>

    <a href="{{ path('app_expense_participant_edit', {'id': expense_participant.id}) }}">edit</a>

    {{ include('expense_participant/_delete_form.html.twig') }}
{% endblock %}
