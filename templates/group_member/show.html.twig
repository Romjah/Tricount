{% extends 'base.html.twig' %}

{% block title %}GroupMember{% endblock %}

{% block body %}
    <h1>GroupMember</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ group_member.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_group_member_index') }}">back to list</a>

    <a href="{{ path('app_group_member_edit', {'id': group_member.id}) }}">edit</a>

    {{ include('group_member/_delete_form.html.twig') }}
{% endblock %}
