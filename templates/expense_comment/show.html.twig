{% extends 'base.html.twig' %}

{% block title %}ExpenseComment{% endblock %}

{% block body %}
    <h1>ExpenseComment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ expense_comment.id }}</td>
            </tr>
            <tr>
                <th>Comment</th>
                <td>{{ expense_comment.comment }}</td>
            </tr>
            <tr>
                <th>DateTime</th>
                <td>{{ expense_comment.dateTime ? expense_comment.dateTime|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_expense_comment_index') }}">back to list</a>

    <a href="{{ path('app_expense_comment_edit', {'id': expense_comment.id}) }}">edit</a>

    {{ include('expense_comment/_delete_form.html.twig') }}
{% endblock %}
