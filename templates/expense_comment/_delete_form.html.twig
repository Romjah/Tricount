<form method="post" action="{{ path('app_expense_comment_delete', {'id': expense_comment.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ expense_comment.id) }}">
    <button class="btn">Delete</button>
</form>
